export const requirementsToDefines = {
    color: "#define COLOR",
    texture: "#define TEXTURE",
    /* For writing a texture directly to the screen without a view-projection matrix */
    texture2D: "#define TEXTURE_2D",
    colorBuffer: "#define COLOR_BUFFER"
}